name: Build & Release backend-fastapi

on:
  workflow_dispatch:
  push:
    branches:
      - "staging"
      - "main"
    paths:
      - "**"
      - ".github/workflows/ryvn-backend-fastapi-release.yaml"
  pull_request:
    branches:
      - "staging"
      - "main"
    paths:
      - "**"

jobs:
  release:
    uses: ryvn-technologies/ryvn-build-action/.github/workflows/release.yml@v2
    with:
      service_name: backend-fastapi
    permissions:
      contents: write
      id-token: write
    secrets:
      RYVN_CLIENT_ID: ${{ secrets.RYVN_CLIENT_ID }}
      RYVN_CLIENT_SECRET: ${{ secrets.RYVN_CLIENT_SECRET }}
